services:
  - type: web
    name: aura-backend
    runtime: docker
    rootDir: backend
    plan: free
    autoDeploy: true
    healthCheckPath: /api/catalog
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: dev
